import { JoinArrayElementsPipe } from './join-array-elements.pipe';

describe('JoinArrayElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinArrayElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
